name: Haazri backend CI/CD

on:
  push:
    branches:
      - v2

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20"

      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 4: Deploy to Server via SSH
      - name: Deploy to Haazri Staging via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 64.227.5.203
          username: root
          key: ${{ secrets.SSH_STAGING_KEY }}
          script: |
            cd /var/www/attendance/stg-haazri/backend
            git pull origin v2
            npm install
            npm build
            pm2 restart stg-haazri-BE --update-env

      # Step 5: Notify Discord on Success
      - name: Notify Discord on Success
        if: success()
        run: |
          curl -X POST -H "Content-Type: application/json" \
          --data "{\"embeds\": [{\"title\": \"🎉 Haazri backend deployed successfully!\", \"description\": \"API is live at [api-stg.haazri.org/docs](https://api-stg.haazri.org/docs)\", \"color\": 3066993}]}" \
          ${{ secrets.STAGING_DISCORD_WEBHOOK_URL }}

      # Step 6: Notify Discord on Failure
      - name: Notify Discord on Failure
        if: failure()
        run: |
          curl -X POST -H "Content-Type: application/json" \
          --data "{\"embeds\": [{\"title\": \"❌ Haazri backend deployment failed!\", \"description\": \"Something went wrong. Check the logs for details.\", \"color\": 15158332}]}" \
          ${{ secrets.STAGING_DISCORD_WEBHOOK_URL }}
